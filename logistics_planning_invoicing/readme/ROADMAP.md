During invoice generation a workaround is added in order to prevent
extra lines without product generation.

This should be properly fixed directly preventing that extra `vals`
dict reception, so it shouldn't be removed later.
